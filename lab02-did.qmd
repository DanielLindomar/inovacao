---
title: "Laboratório 2 — Diferenças-em-diferenças"
subtitle: "O incentivo aumentou mesmo o investimento em P&D?"
author: "Prof. Dr. Lindomar Pegorini Daniel — UNEMAT, Campus de Sinop"
engine: knitr
---

::: {.logo-topo}
![](logo-unemat.png){width="110"}
:::

::: {.callout-note}
## Objetivos de aprendizagem

Ao final deste laboratório você será capaz de:

- ler a estimativa de diferenças-em-diferenças construída a partir de quatro médias;
- reconhecer a mesma estimativa numa tabela de regressão;
- interpretar o coeficiente de interesse e a sua incerteza;
- **verificar se o método acertou**, comparando com o efeito verdadeiro;
- explicar em que condição o método deixaria de funcionar.
:::

## O que já sabemos

No [Laboratório 1](lab01-explorando-dados.qmd) você descobriu duas coisas incômodas: as empresas que aderem ao incentivo **já gastavam mais antes**, e a diferença entre os grupos no período final embute essa vantagem prévia.

Aqui separamos as duas parcelas. Rode o primeiro bloco antes dos demais — e, como no Laboratório 1, não há nada para digitar: cada bloco vem pronto, e o que se pede é ler a saída.

```{webr}
dados <- read.csv("dados/fomento_sim.csv")
medias <- aggregate(pd ~ beneficiaria + pos, data = dados, FUN = mean)
medias
```

## As duas diferenças, na mão

Vamos guardar as quatro médias em objetos com nomes claros e fazer a conta passo a passo.

```{webr}
b_antes  <- medias$pd[medias$beneficiaria == 1 & medias$pos == 0]
b_depois <- medias$pd[medias$beneficiaria == 1 & medias$pos == 1]
n_antes  <- medias$pd[medias$beneficiaria == 0 & medias$pos == 0]
n_depois <- medias$pd[medias$beneficiaria == 0 & medias$pos == 1]

var_beneficiarias     <- b_depois - b_antes    # 1a diferenca: variacao dos tratados
var_nao_beneficiarias <- n_depois - n_antes    # 1a diferenca: variacao dos controles
did <- var_beneficiarias - var_nao_beneficiarias   # 2a diferenca

cat("Variação das beneficiárias:     ", round(var_beneficiarias, 1), "\n")
cat("Variação das não beneficiárias: ", round(var_nao_beneficiarias, 1), "\n")
cat("Diferenças-em-diferenças:       ", round(did, 1), "\n\n")
cat("Comparação bruta (só o depois): ", round(b_depois - n_depois, 1), "\n")
cat("Viés de seleção (só o antes):   ", round(b_antes - n_antes, 1), "\n")
```

::: {.callout-tip}
## Como ler essa saída

Confira a decomposição: a **comparação bruta** é igual ao **viés** mais o **diferenças-em-diferenças**. Não é coincidência nem aproximação — é identidade aritmética.

E veja a proporção. A maior parte do que a comparação bruta chamaria de "efeito do programa" é, na verdade, a diferença que já existia entre os grupos antes de o programa começar.
:::

### A decomposição, conferida

O bloco soma o viés e a estimativa de diferenças-em-diferenças e compara com a diferença bruta.

```{webr}
vies  <- b_antes - n_antes
bruta <- b_depois - n_depois

cat("viés + did =", round(vies + did, 1), "\n")
cat("bruta      =", round(bruta, 1), "\n")
```

::: {.callout-tip}
## Como ler essa saída

Os dois valores são idênticos: 254,6 + 138,0 = 392,6. A comparação bruta é a soma de duas coisas — e só uma delas tem a ver com o programa. Quase dois terços do que a manchete chamaria de "efeito" são a diferença que já existia em 2022.
:::

## A mesma conta, por regressão

Fazer na mão funciona com dois grupos e dois períodos. Com mais grupos, mais períodos ou variáveis de controle, usa-se regressão — e o resultado é exatamente o mesmo.

A ideia: explicar `pd` por três termos. Um indica o grupo, outro indica o período, e o terceiro — o **produto dos dois** — indica estar tratado *e* depois. É esse último que carrega o efeito.

```{webr}
modelo <- lm(pd ~ beneficiaria + pos + beneficiaria:pos, data = dados)
summary(modelo)
```

::: {.callout-tip}
## Como ler essa tabela

Quatro linhas na coluna `Estimate`, e cada uma tem um significado concreto:

- **`(Intercept)`** — a média das não beneficiárias antes. É o ponto de partida.
- **`beneficiaria`** — quanto as beneficiárias já estavam acima, antes. **É o viés de seleção.**
- **`pos`** — quanto as não beneficiárias variaram no tempo. É a tendência comum, o que aconteceu com todo mundo.
- **`beneficiaria:pos`** — **o efeito estimado do programa.** É o mesmo número que você calculou na mão.

Compare cada coeficiente com os números da seção anterior. Eles batem.

Na coluna `Pr(>|t|)`, o valor do termo de interação indica a probabilidade de observar um efeito desse tamanho se o efeito verdadeiro fosse zero. Valores pequenos sugerem que o resultado não é fruto do acaso amostral.
:::

### O intervalo de confiança

Uma estimativa pontual esconde a incerteza. A função `confint()` devolve, para cada coeficiente, a faixa de valores compatível com os dados.

```{webr}
confint(modelo)
```

::: {.callout-tip}
## Como ler essa saída

Olhe a linha `beneficiaria:pos`: o intervalo vai de cerca de **101 a 175 mil reais**. A estimativa central é 138, mas os dados são compatíveis com um efeito bem menor ou bem maior — algumas dezenas de milhares para cada lado. É honesto reportar essa faixa, e não apenas o valor central; um relatório que diz "o programa aumentou o P&D em 138 mil" está fingindo uma precisão que não tem.
:::

## A verificação que nenhuma avaliação real permite

```{webr}
EFEITO_VERDADEIRO <- 150   # em R$ mil, usado para gerar esta base

cat("Efeito verdadeiro:        ", EFEITO_VERDADEIRO, "\n")
cat("Estimativa por DiD:       ", round(did, 1), "\n")
cat("Erro da estimativa:       ", round(did - EFEITO_VERDADEIRO, 1), "\n\n")
cat("Comparação bruta:         ", round(b_depois - n_depois, 1), "\n")
cat("Erro da comparação bruta: ", round((b_depois - n_depois) - EFEITO_VERDADEIRO, 1), "\n")
```

::: {.callout-important}
## O que acabou de acontecer

O efeito verdadeiro, usado para construir esta base, é **150 mil reais**.

A comparação bruta erra por uma margem enorme — atribui ao programa quase o triplo do que ele fez. O diferenças-em-diferenças chega perto do valor correto, com um resto pequeno que é apenas ruído amostral.

Nenhuma avaliação com dados reais permite essa conferência: o efeito verdadeiro nunca é conhecido. É por isso que se estuda o método com dados simulados — para ver, uma vez, que ele funciona.

E repare que o DiD **não acerta na mosca**. Erra um pouco, e erraria diferente com outra amostra. É exatamente o que o intervalo de confiança do bloco anterior estava dizendo — e repare que o valor verdadeiro, 150, está dentro dele.
:::

### A estimativa ingênua, com aparência de rigor

O bloco roda uma regressão usando **apenas os dados do período final**, explicando `pd` por `beneficiaria`. É a versão "modelo estatístico" da comparação bruta.

```{webr}
depois <- dados[dados$pos == 1, ]
modelo_ingenuo <- lm(pd ~ beneficiaria, data = depois)
summary(modelo_ingenuo)
```

::: {.callout-tip}
## Como ler essa tabela

O coeficiente de `beneficiaria` é 392,6 — a comparação bruta, reproduzida com três estrelas de significância, erro-padrão pequeno e um valor-p minúsculo. Tudo tecnicamente correto, e a resposta continua errada por mais de 240 mil reais.

**Guarde isto:** uma regressão pode estar impecável e responder à pergunta errada. Significância estatística não é evidência de causalidade. Quando um relatório apresentar uma tabela assim, a pergunta a fazer é a do Laboratório 1: os grupos já eram diferentes antes?
:::

## E se as tendências não fossem paralelas?

O método depende de uma hipótese: sem o programa, os dois grupos teriam evoluído em paralelo. Vamos ver o que acontece se isso for falso.

O bloco abaixo cria uma base artificial em que as beneficiárias recebem, **além** do efeito do programa, um empurrão extra de 200 que nada tem a ver com ele — um choque setorial, digamos, uma alta de preço que atingiu só aquele grupo.

```{webr}
falso <- dados
extra <- 200
sel <- falso$beneficiaria == 1 & falso$pos == 1
falso$pd[sel] <- falso$pd[sel] + extra

m2 <- aggregate(pd ~ beneficiaria + pos, data = falso, FUN = mean)
did_falso <- (m2$pd[m2$beneficiaria==1 & m2$pos==1] - m2$pd[m2$beneficiaria==1 & m2$pos==0]) -
             (m2$pd[m2$beneficiaria==0 & m2$pos==1] - m2$pd[m2$beneficiaria==0 & m2$pos==0])

cat("Efeito verdadeiro do programa:", EFEITO_VERDADEIRO, "\n")
cat("DiD nesta base contaminada:   ", round(did_falso, 1), "\n")
```

::: {.callout-warning}
## O método não protege contra isso

O diferenças-em-diferenças elimina tudo o que era **constante** em cada grupo. Não elimina o que muda de forma **diferente** entre os grupos no período.

Aqui ele atribuiu ao programa um efeito de cerca de 340 quando o verdadeiro é 150 — e não há nada nos números que denuncie o problema. A tabela sai bonita, o coeficiente é significante, e a conclusão é falsa.

A única defesa é conhecer o contexto: **o que mais aconteceu nesse período que atingiu um grupo e não o outro?** É uma pergunta de gestor, não de estatístico. E é por isso que avaliação séria exige quem conheça o setor.
:::

## Antes de sair

::: {.callout-note}
## O que você deve conseguir explicar depois deste laboratório

1. Por que a comparação bruta é igual ao viés mais o efeito — e o que isso significa na prática.
2. O que cada um dos quatro coeficientes da regressão representa.
3. Uma situação concreta, no seu setor, em que a hipótese de tendências paralelas seria implausível.
:::
